# Web Engineering
The module is written in java spring boot ecosystem.

**To get started:**
- install java
- install spring-boot
- move inside demo folder
- run
```
mvn clean install -x
mvn spring-boot:run
```
